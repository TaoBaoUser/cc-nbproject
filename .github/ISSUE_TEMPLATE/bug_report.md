---
name: 缺陷报告
about: 报告一个可复现的问题
title: ''
labels: bug
assignees: ''
---

<!--
⚠️ 请勿粘贴真实的 API key 或准入凭证。
   报错信息里若含凭证，请用「长度 + 前几位」代替，例如 sk-abc…（51 字符）。
-->

## 环境

- 操作系统：
- Node 版本（`node -v`）：
- 本工具版本（见 `package.json` 的 `version`）：
- 是通过 `npm start` 运行，还是打包后的应用：

## 现象

<!-- 发生了什么，你期望发生什么。 -->

## 复现步骤

1.
2.
3.

## 关键信息

**接管状态**（界面侧边栏显示的是「已接管 / 已授权未生效 / 未接管」中的哪一个）：

**代理是否在运行、端口是多少**（界面显示，或 `~/.cc-nbproject/profiles.json` 的 `settings.port`）：

**相关日志**：

```
（粘贴）
```

## 是否已尝试

- [ ] 点过「连接测试」，结果是：
- [ ] 点过「断开接入」（这会把 Claude Code 配置还原）
- [ ] 重启过应用
