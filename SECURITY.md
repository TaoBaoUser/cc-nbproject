# 安全政策

## 报告漏洞

**请不要用公开 issue 报告安全漏洞。** 公开 issue 会让所有使用者在你修复之前暴露在风险里。

请用 GitHub 的**私密漏洞报告**：
<https://github.com/TaoBaoUser/cc-nbproject/security/advisories/new>

这是唯一可靠的渠道 —— 项目没有专门的邮箱，而私密报告能在修复发布前保住披露的时机。

会在 72 小时内确认收到，并在修复发布后致谢（如果你愿意署名）。

## 这个项目为什么需要一份安全政策

本工具会**改写用户的 `~/.claude/settings.json`**，并在本机跑一个反向代理。
写错的代价不是「功能不工作」，而是**用户的 Claude Code 突然不能用了，而用户完全想不到
是本工具造成的**。所以下面这些边界是设计的一部分，不是缺陷。

### 设计如此（不是漏洞）

- **API key 以明文存放在 `~/.cc-nbproject/profiles.json`**（文件 `0600`，目录 `0700`）。
  v0.1 不做加密 —— 加密所需的主密钥同样得存在本地，只是把问题挪了一层。
- **代理只监听 `127.0.0.1`。** 但「本机」不等于「本用户」，所以另有一层准入校验：
  代理只接受本机生成的 32 字节随机凭证，凭据不符时回 `401` 且**不向上游发请求**。
- **写进 Claude Code 配置的是本地准入凭证，不是你的真实 API key。**
  这样 Claude Code 那边不必知道真实 key，切换供应商才能不重启。
- **接管与还原是成对的**：点一次「确认接管」长期记住，此后开应用自动接管、
  关应用自动还原。还原只碰 `ANTHROPIC_BASE_URL` 和 `ANTHROPIC_AUTH_TOKEN` 两个键，
  你自己手改过的键不会被覆盖。
- **应用关闭时代理不运行。** 不再使用本工具前请先点「断开接入」，
  否则 Claude Code 会指向一个没人监听的端口。

### 属于漏洞（请报告）

- 代理在**未持有有效准入凭证**时仍然转发请求，或把请求发给了上游
- 接管后退出时不还原；还原时覆盖了用户手改的键；或还原失败却不告诉用户
- 写回 `~/.claude/settings.json` 时放宽了文件权限（应当始终是 `0600`）
- 代理启动失败时，把一个连不上的地址写进了用户配置
- 渲染进程能绕过 preload 白名单触达主进程能力
- 任何能把用户真实 API key、或本地准入凭证，泄漏给第三方的路径
- 配置文件损坏时整个主进程崩溃（应当转成可读的 `500`，应用存活）

## 支持范围

只支持最新发布版本。v0.1 明确不做：故障转移、系统托盘、应用打包分发、配置加密。

已知限制见 [CHANGELOG 的「已知限制」](CHANGELOG.md)，安全相关的历史缺陷与处置见
[docs/审计发现与处置.md](docs/审计发现与处置.md)。
