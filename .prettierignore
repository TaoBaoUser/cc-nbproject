# prettier 不该碰的东西。分三类，每类都有具体理由。
#
# 为什么要有这个文件：`prettier --write .` 默认会扫过仓库里的一切，
# 包括那些「格式统一」并不是优点的文件。下面这些格式化它们只会制造噪音，
# 甚至篡改本应保持原样的记录。

# --- 1. 本地私有配置 ---
# .claude/settings.local.json 是 Claude Code 在本机维护的权限白名单，
# 属于「这台机器」而不属于「这个仓库」（已被全局 gitignore 排除）。
# 它既不会被提交，也就不需要为团队统一格式。
.claude/

# --- 2. 规则文件本身 ---
# CLAUDE.md 是写给 AI 的执行规则，且明确声明「冲突时以本文件为准」。
# 它里面的表格与引用块排版是刻意的；重排它只会产出一个纯噪音 diff，
# 还得顺带重验一遍规则有没有在重排中被改坏 —— 风险不为零，收益为零。
CLAUDE.md

# --- 3. 历史归档 ---
# docs/plans/ 下是过去某次工作的 PRD 快照。归档件的价值恰恰在于
# 「它当时长什么样」，重新格式化等于篡改历史记录。
docs/plans/

# --- 4. 双保险 ---
# 这两项 .gitignore 已经排除了，且 prettier 默认会读 .gitignore。
# 写在这里是为了「有人改了 .gitignore 也不会误伤」。
node_modules/
dist/
