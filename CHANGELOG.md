# 变更日志

本文件记录本项目所有值得注意的变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> **给写这个文件的人**：新增条目请写「用户能观察到什么」，不要写「改了哪个函数」。
> 内部重构若对使用者无感，归入 `### 内部`。

---

## [未发布]

### 新增

- **CI 工作流**（`.github/workflows/ci.yml`）：第 1 级（类型 + 规范 + 格式 + 单测 + 构建）
  与第 2 级（真 Electron 端到端）都挂在每次 push 与 PR 上 —— 本仓库是公开仓库，
  标准 runner 的用量免费且不限分钟
- **主进程纳入类型检查**（`tsconfig.main.json`）：此前 `tsc` 只覆盖渲染进程，
  约 6000 行主进程代码（含**唯一会改用户配置**的 `setup.js`）完全在检查盲区。
  采用渐进策略 —— `checkJs: false` 配合逐文件 `// @ts-check`，
  避免 CI 一上来就因存量报错全红
- **推送前门禁**（`.githooks/pre-push`）：推送前先跑一遍 `npm run verify`，
  不必等 CI 变红再回头改
- `.editorconfig`、`.nvmrc`、`.prettierignore`：统一编辑器行为、Node 版本，
  以及**不该被格式化**的三类边界（本地私有配置 / 规则文件 / 历史归档）
- [CONTRIBUTING.md](CONTRIBUTING.md)：贡献者入口，含十条「不能碰的线」
- [docs/项目文档.md](docs/项目文档.md)：项目总文档，八章收齐「是什么、怎么用、怎么改、怎么验、踩过什么坑」
- Pull Request 与 Issue 模板
- `npm run verify`：一键跑完第 1 级验证（typecheck + lint + test）

### 修复

- **`profiles.json` 损坏会带崩整个应用**：代理的准入校验直接读该文件而没有兜底，
  文件内容非法或读取失败就抛在主进程里。改为 **fail-closed** —— 回一条可读的 `500`
  并拒绝服务，**绝不降级成「跳过校验」**：后者会让「把配置文件弄坏」变成
  同机任意进程绕过准入凭证的办法，那不是容错而是漏洞
- **`npm run dev` 在设置了 `ELECTRON_RUN_AS_NODE` 的终端里必然启动失败**：
  该变量会让 Electron 退化成普通 Node 进程（`app` 为 `undefined`），
  而报错看上去像主进程的 bug。现已在 spawn 前清除它

### 内部

- `scripts/` 重组：e2e 的 driver 与它启动的 Electron fixture 收进 `scripts/e2e/`
- 全仓按 Prettier 格式化 —— 此前配置存在但从未执行过，25 个文件不符合仓库自己的规范
- 补齐 `.gitignore` 缺口：`.claude/settings.local.json`（此前只靠作者机器的全局配置挡住）
- 订正 `docs/` 中的事实性描述（Node 版本口径、类型检查的覆盖范围）

> `docs/plans/` 下的历史 PRD 刻意保持原样：未重新格式化，也未更新其中的路径引用。
> 归档件的价值在于记录「当时是什么样」。

---

## [0.1.0] - 2026-09-22

首个版本。核心能力：**让 Claude Code 切换 API 供应商时不需要重启**。

### 新增

- **本地反向代理**：`ANTHROPIC_BASE_URL` 恒为本机地址（`8787` 起，被占则向上顺延至 `8807`），
  切换供应商只改代理的路由目标，正在运行的 Claude Code 会话不受影响
- **桌面控制台**（Electron + React）：供应商的增删改查、一键切换、连接测试（含延迟）
- **接管与还原**：点一次「确认接管」即长期记住，此后每次启动自动生效、
  **每次退出自动还原**；随时可「断开接入」。还原只针对
  `ANTHROPIC_BASE_URL` 与 `ANTHROPIC_AUTH_TOKEN` 两个键
- **本地准入凭证**：代理只接受本机生成的 32 字节随机凭证，认不出即回 `401` 且不打上游
- **模型映射**：不同供应商对同一模型的叫法不同，按「源=目标」逐行填写，未命中的模型名原样转发
- **模型列表拉取**：从供应商拉取可选模型 ID 自动预填映射（辅助功能，供应商没有该端点时可手动填写）
- **实时请求日志**：每个请求走了哪个供应商、状态码、耗时
- **用量统计**：token 消耗与请求数
- **系统代理支持**：上游请求自动走 macOS 系统代理设置（`scutil --proxy`），回环与内网地址始终直连

### 安全

- API key 明文存储于 `~/.cc-nbproject/profiles.json`（权限 `0600`，目录 `0700`）。
  v0.1 不自研加密——加密所需的主密钥同样得存在本地，只是把问题挪了一层
- 代理只监听 `127.0.0.1`。但「本机」不等于「本用户」，故引入上面的准入凭证
- 渲染进程启用 `contextIsolation` 与 `sandbox`、禁用 `nodeIntegration`，
  经 `preload` 白名单访问主进程能力（白名单而非透传）
- 写回 `~/.claude/settings.json` 时显式保持 `0600`，避免原子写入把权限放宽成 `0644`
- 本工具**替代** Claude Code 保存真实 API key：写进 Claude Code 配置的是准入凭证

### 修复

首个版本前经过一轮对抗性审查，六条高危缺陷已在发布前修复
（完整判定与处置见 [docs/审计发现与处置.md](docs/审计发现与处置.md)）：

- 配置损坏会带崩整个应用 → 转成可读的 `500`，应用存活
- 代理启动失败时把必然连不上的地址写进用户配置 → 改为一律取自实听地址
- 代理启动失败后无法重试 → 修正 `start()` 的状态清理
- 接管单向、退出不还原、无还原入口 → 补全为双向可逆
- 写回配置时权限被放宽到 `0644` → 原子写入全程保持 `0600`
- 同机任意进程可白嫖真实 API key → 改为随机准入凭证

### 已知限制

- 暂不支持：故障转移、系统托盘、应用打包分发、配置加密
- 应用关闭时代理不运行，此时 Claude Code 会指向无人监听的端口 ——
  不再使用本工具前请先「断开接入」

[未发布]: https://github.com/TaoBaoUser/cc-nbproject/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/TaoBaoUser/cc-nbproject/releases/tag/v0.1.0
