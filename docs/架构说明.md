# 架构说明

> 代码地图与改动指引。读它之前建议先看[项目速览](./项目速览.md)（一页硬事实），
> 读完之后按需查阅[设计文档](./plans/2026-09-21-cc-nbproject-design.md)
> （取舍的完整记录）与[工程约束](./工程约束.md)（规范）。
>
> 最后核对：2026-09-22

---

## 1. 全景

```
┌─────────────────────── Electron 应用 ───────────────────────┐
│                                                              │
│  主进程 (src/main)                    渲染进程 (src/renderer) │
│  ┌────────────────────┐               ┌──────────────────┐   │
│  │ index.js  装配/生命周期│              │ App.tsx          │   │
│  │  ├ proxy.js  ★代理   │◄── IPC ──────┤  hooks/          │   │
│  │  ├ setup.js  ★接管   │  (白名单)     │  views/          │   │
│  │  ├ store.js  配置    │               │  components/     │   │
│  │  ├ models.js 模型列表 │               │  modals/         │   │
│  │  └ usage.js  用量    │               └──────────────────┘   │
│  └────────────────────┘                        ▲              │
│           ▲                                    │              │
└───────────┼────────────────────────────────────┼──────────────┘
            │ 转发                                │ contextBridge
            │                                     │ (preload/index.js)
   ┌────────┴─────────┐                  ┌────────┴─────────┐
   │ 上游供应商 API    │                  │ Claude Code      │
   │ (DeepSeek/Kimi…)  │                  │ (命令行/IDE 里的) │
   └───────────────────┘                  └──────────────────┘
                                            ANTHROPIC_BASE_URL
                                            = 本机代理
```

**关键边界**：代理跑在**主进程**。渲染进程是沙箱化的浏览器环境（`sandbox: true`、
`nodeIntegration: false`、`contextIsolation: true`），不适合监听端口；
主进程才是完整的 Node 运行时。

**第二条边界**：`proxy.js` / `setup.js` / `store.js` / `models.js` / `usage.js`
**都不依赖 Electron**。这是 `npm test` 能在几百毫秒内跑完、完全不需要 GUI 的原因。

---

## 2. 一次请求的完整路径

```
Claude Code
  │ POST http://127.0.0.1:8787/v1/messages
  │ Authorization: Bearer <localToken>
  ▼
proxy.handleRequest                                      [proxy.js:290]
  ├─ 1. 准入校验  holdsLocalToken(req, getLocalToken())
  │       不过 → 401 {"type":"unauthorized"}，**不打上游**
  ├─ 2. getActiveProfile()  （包在 try/catch 里）
  │       抛错 → 500 config_unreadable；为 null → 503 no_active_profile
  ├─ 3. 解析 profile.baseUrl，拼路径 basePath + clientReq.url
  │       baseUrl 非法 → 500 invalid_base_url
  ├─ 4. 选传输：需要走系统代理时用 proxy-agent.js，否则直连
  ├─ 5. 模型映射（仅命中时才重新序列化请求体）
  │       rewriteModel() 命中 → 改写 model 字段；未命中 → 原样透传
  ├─ 6. 换掉凭证：发往上游的是 profile.apiKey，客户端的凭证被丢弃
  ├─ 7. 流式判定（content-type 含 text/event-stream）
  │       ├─ 是 → SSE：逐块透传，不缓冲；顺路抽 usage
  │       └─ 否 → 收全再转发，从响应体抽 usage
  └─ 8. onUsage / onRequest 回调 → index.js 广播到界面
```

**为什么模型映射「未命中就返回 null」**：命中才需要重新序列化整个请求体。
未命中时把原始 Buffer 直接转发，既省一次解析，也保证非 JSON 请求体
（比如某些附件流）不被破坏。见 `rewriteModel` 的返回值约定。

---

## 3. 模块逐个说明

### `src/main/index.js`（473 行）—— 装配与生命周期

**职责**：创建窗口、装配各模块、注册全部 IPC、编排接管/还原、管理退出。

**不变量**：

| 不变量 | 位置 |
|---|---|
| 单实例锁在**模块顶层**判定，落败分支不做任何还原 | 文件顶部 |
| `bootstrap()` 包裹全部副作用（失败分支不许生成 `profiles.json`） | `bootstrap()` |
| 接管/断开用 `takeoverBusy` 串行，后来者直接拒绝（不排队） | `withTakeoverLock` |
| 接管失败时**绝不**写 `enabled = true`（R11 写序） | `performTakeover` |
| 断开时**先还原、后清授权记忆** | `performDisconnect` |
| 还原只在 `before-quit` 做一次，然后 `app.exit(0)` | 文件末尾 |

**为什么这些实例不放在模块顶层**：失败分支一旦执行到那里，会在用户毫不知情的情况下
生成 `~/.cc-nbproject/profiles.json` 并写入本地准入凭证。一个「什么都不做」的退出
不该有这种副作用。

### `src/main/proxy.js`（938 行）—— 代理核心

**职责**：`http.createServer` + 转发 + SSE + 用量提取 + 连接测试。

**对外**：`createProxy({ getActiveProfile, getLocalToken, onUsage, onRequest, host, port })`
→ `{ start, stop, port, host, baseUrl }`。

**要点**：

- **`baseUrl` 是 getter**，取自实听端口。端口被占时会向上顺延（8787→8807，最多 20 次）。
  任何需要写地址给用户的地方都必须用它，**不能写死 8787**。
- **`stop({ timeoutMs })` 有超时上界**。`server.close()` 要等所有连接结束，
  而转发路径没有超时 —— 用户开着 SSE 会话时这个等待没有上界，而退出流程会
  `await` 它，表现为「关掉窗口后应用再也退不掉」。超时后 `closeAllConnections()`。
- **`start()` 失败时必须把 `server` 清回 `null`**。否则下一次 `start()` 会命中
  `if (server) return actualPort` 直接返回 `null` —— 不报错也不重试。
- 同文件的 `testUpstream(profile)` 是「连接测试」按钮的实现，
  与转发路径共用 `rewriteModel` / `summarizeUpstreamError`。

### `src/main/proxy-agent.js`（338 行）—— 上游请求走系统代理

**职责**：Node 的 `http`/`https` 既不读 macOS 的「网络」设置，也不读 `HTTPS_PROXY`
（除非显式开启），所以主进程的请求永远直连。上游按来源地区限制时
（例如 OpenRouter 的 `openai/*` 一族），用户明明开着代理却连不上。

**要点**：macOS 走 `scutil --proxy` 探测（结果缓存 60 秒，系统代理可能随时开关）；
回环与内网地址**永远直连** —— 把它们交给代理是错的。

### `src/main/setup.js`（578 行）—— **唯一会改用户文件的模块**

**职责**：接管 / 还原 `~/.claude/settings.json`、读模型名、检测企业强制配置。

**对外**：`createSetup({ claudeDir, localToken })` →
`{ claudeDir, settingsPath, previewClaudeSettings, applyClaudeSettings,
   restoreClaudeSettings, readClaudeModelNames, readCurrentSettings, detectManagedConflict }`

**两条贯穿全文件的规矩**（写在文件头）：

1. **预览与应用必须分开**，中间由用户显式确认。任何静默修改都是不可接受的。
2. **接管必须是双向的**。只接不管，用户关掉应用后 Claude Code 就指向一个没人监听的端口。

**受管键只有两个**：`ANTHROPIC_BASE_URL`、`ANTHROPIC_AUTH_TOKEN`。
模型相关键（`ANTHROPIC_MODEL` 等）是用户对自己所用模型的配置，与走不走代理无关。

**三处最容易写错的地方**（都有测试钉死）：

| 位置 | 坑 |
|---|---|
| `looksLikeOurOwnValue` | 判定「这个值是不是我们自己写的」。**宁可不认，也不能误认** —— 误认会把用户真实配置当成残渣丢掉 |
| `applyClaudeSettings` 的 `previous` 计算 | 残渣绝不能记进 `previous`，否则还原会把死值写回给用户 |
| `restoreClaudeSettings` 的三态判定 | 见下 |

### `src/main/store.js`（296 行）—— 配置持久化

**职责**：读写 `~/.cc-nbproject/profiles.json`。**原子写入**（tmp → rename → chmod），
权限 `0600`。

**为什么原子**：进程在写入中途崩溃（或磁盘写满）时，目标文件会变成半截 JSON，
下次启动 `JSON.parse` 抛错，工具彻底起不来 —— 而那里面有用户所有的 API key。

**新增的部分**：`getLocalToken()`（本地准入凭证）、`getTakeover()` / `setTakeover()` /
`patchTakeover()`（接管状态，合并式更新）。

### `src/main/models.js` / `usage.js`

- `models.js`：从供应商拉 `/v1/models` 并归一化（三种响应形状都容忍），带缓存。
  **这是辅助功能**：Claude Code 的兼容接口规范里并没有 `/v1/models`，
  不少供应商没有这个端点，手动填写映射始终可用。失败要给出可读的分类
  （`empty` / `auth_failed` / `network_error`…），不要只丢一个 HTTP 状态码。
- `usage.js`：`usage.jsonl` 追加与聚合。选 JSONL 而非 JSON 数组是为了 O(1) 追加。

### `src/preload/index.js` —— 白名单

**关键点：白名单而非透传。** 绝不能写成
`exposeInMainWorld('ipcRenderer', ipcRenderer)` —— 那等于把整个 IPC 面暴露给页面脚本，
而页面会显示来自 API 响应的内容。

新增能力必须**三处同步**：`index.js` 的 `ipcMain.handle` → 这里的白名单方法 →
`types.ts` + `ccnb.ts` 的类型。

### `src/renderer/` —— React 界面

```
App.tsx            顶层：视图切换 + 模态框开关
hooks/             每个 hook 对应一块主进程状态
  useProfiles     供应商列表
  useProxyStatus  代理运行状态
  useClaudeStatus 接管三态（新增）
  useLogs         实时日志（订阅 log:event）
  useUsage        用量
views/             ProvidersView / ProviderCard / LogsView / UsageView
components/        Sidebar / ModelPicker / Combobox
modals/            ProfileModal / SetupModal
describe.ts       把连接测试与模型拉取的原始结果翻译成用户能照着做的话
types.ts          ★ 所有跨进程数据结构的唯一来源
model-map.js      模型映射文本的解析（**刻意留在 .js**：要能被 node --test 直接 require）
```

**`describe.ts` 为什么单独存在**：主进程返回的是分类码，界面上要变成
「你的 key 无效，请检查供应商后台」这样的话。翻译逻辑集中在 `describe.ts`，
视图里不该出现 `if (kind === 'auth_failed')`。

---

## 4. 接管/还原的状态机

这是本仓库最需要小心的一条线。完整规格见
[接管与还原 PRD](./plans/2026-09-21-接管与还原-prd.md)。

### 持久化形状

```jsonc
// ~/.cc-nbproject/profiles.json 的 settings.takeover
{
  "enabled": true,                    // 用户授权过长期接管
  "applied":  { "ANTHROPIC_BASE_URL": "http://127.0.0.1:8788", "ANTHROPIC_AUTH_TOKEN": "…" },
  "previous": { "ANTHROPIC_BASE_URL": "https://api.deepseek.com/anthropic", "ANTHROPIC_AUTH_TOKEN": "sk-…" },
  "fileExisted": true,                // 接入前 settings.json 是否存在
  "backupPath": "/Users/…/settings.json.bak.2026-09-22T…",
  "lastApply":   { "ok": true,  "at": "…", "reason": null },
  "lastRestore": { "ok": true,  "at": "…", "reason": null }
}
```

`previous[key] === null` 的含义是「**该键原本不存在**」，还原时要把它删掉 ——
不是写成空串。

### 状态转换

```
        ┌──────────────────────┐
        │ off  未接管           │
        └──────────┬───────────┘
                   │ 用户点「确认接管」
                   │ 准入检查：代理在监听？有激活的供应商？
                   ▼
        ┌──────────────────────┐
        │ active 已接管         │◄──── 应用每次启动自动回到这里
        │ 文件里确实是我们写的值  │
        └──────────┬───────────┘
                   │
        ┌──────────┴───────────┐
        │ 应用退出 → 还原        │──► 文件回到 previous，enabled 保留
        │ 点「断开接入」→ 还原    │──► 文件回到 previous，takeover 清空
        └──────────────────────┘

        ┌──────────────────────┐
        │ pending 已授权未生效   │  授权过，但本次没写进去
        │ （代理没起来 / 写失败） │  界面要显示 lastApply.reason
        └──────────────────────┘
```

**`active` 与 `pending` 的区分是界面上的要害。** 两者都「授权过」，差别在于
**本次到底写没写进用户的文件**。把 `pending` 显示成「已接管」会让用户在
Claude Code 连不上时完全摸不着头脑 —— 界面说一切正常，配置里却还是旧地址。

判定以**文件内容**为准（`buildTakeoverStatus` 逐键比对 `takeover.applied`），
`enabled` 只作旁证。

### 还原的三态判定（`restoreClaudeSettings`）

| 当前值 | 含义 | 动作 |
|---|---|---|
| 等于 `applied[key]` | 是我们写的，没被动过 | 回滚为 `previous[key]`（为 `null` 则删键） |
| 等于 `previous[key]` | 已是目标状态（刚还原过） | 不写、不标记（**幂等**） |
| 其它 | 用户自己改过 | 保持不动，标记进 `externallyModified` |

第二条分支是幂等的落点。没有它，第二次还原会把「我们刚写回去的原值」
误报成「用户改过」，产生假告警。

---

## 5. 「我想做 X → 改哪里」

| 想做的事 | 改哪里 | 注意 |
|---|---|---|
| 加一个供应商字段 | `store.js` 的 `addProfile`/`updateProfile` 白名单 + `types.ts` + `ProfileModal` | `updateProfile` 的 `allowed` 是显式白名单，别忘了加 |
| 加一个界面视图 | `App.tsx` 的 `NAV`/`ViewName` + `views/` + `Sidebar.tsx` 的 `NAV` | 两处 `NAV` 都要改 |
| 加一个 IPC 能力 | 主进程 → preload → types 三处 | 见 §3 preload |
| 调整转发行为 | `proxy.js` 的 `handleRequest` | 先想清楚会不会破坏 SSE 逐块透传 |
| 改接管/还原语义 | `setup.js` + `index.js` | 先读 PRD 的 R1–R11，再跑 `test/setup.test.js` 第 10、11 条 |
| 改准入凭证 | `store.getLocalToken` + `proxy.holdsLocalToken` + `setup.buildNextSettings` | 三处是同一个值的生产/校验/写入，必须一起改 |
| 新增一个交付给用户的文案 | `describe.ts`（界面）或 `ok:false` 的 `reason`（主进程） | 文案要能照着做，不要出现内部术语 |

---

## 6. 测试与检查的分工

| 工具 | 覆盖 | 什么时候跑 |
|---|---|---|
| `npm test`（123 个用例） | 纯逻辑、文件读写（隔离目录）、代理转发（本地假上游） | 每次改动 |
| `npx tsc --noEmit` | 渲染进程与 preload 的类型 | 每次改动 |
| `npm run lint` | 代码规范、React hooks 规则 | 每次改动 |
| `npm run e2e` | **真 Electron**：接管 → 校验文件 → 真实退出路径 → 校验还原 | 改 `index.js` 生命周期或渲染进程 |
| `npm run smoke` | **真实窗口**：连用户正在用的实例去点界面 | 改界面交互 |

三个测试文件对应三块最容易出错、且出错最难发现的逻辑：

- `test/proxy.test.js`（923 行）—— 静默出错的问题（用量算错、鉴权用错 key）
  与破坏核心体验的问题（把流式响应缓冲成一整块）
- `test/setup.test.js`（651 行）—— 接管与还原，全部在 `mkdtemp` 隔离目录上
- `test/store.test.js` —— 接管状态与准入凭证的持久化
