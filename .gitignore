# --- 依赖 ---
node_modules/

# --- 构建产物 ---
dist/
out/
build/
*.asar

# --- 敏感信息：绝不允许进入仓库 ---
# 供应商配置里含有真实 API key，运行时存放在用户目录（~/.cc-nbproject/），
# 这里再加一层兜底，防止有人把本地副本放进项目目录后误提交。
.env
.env.*
*.key
profiles.json
profiles.local.json
usage.jsonl
secrets/
credentials/
**/settings.json.bak.*

# --- 编辑器 / 系统 ---
.DS_Store
.vscode/
.idea/
*.swp

# --- 本地工具配置 ---
# Claude Code 的本地权限白名单。它记录的是「这台机器上放行过哪些命令」，
# 属于个人环境而不属于这个项目 —— 提交上去只会给协作者制造无谓冲突。
# 作者机器的全局 gitignore 恰好覆盖了它，但别人克隆时不会继承那份配置，
# 所以仓库自己要有一条。
.claude/settings.local.json

# --- 日志 ---
*.log
npm-debug.log*
logs/
